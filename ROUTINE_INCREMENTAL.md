# 差分巡回への指示

GUI から追加された作品・監督と、手動の再検索予約だけを拾う。毎時 0 分に発火する（8 時台を除く）。
`create_trigger` に渡しているプロンプトの実体もこの内容。手順を変えたいときは
ここと Routine の両方を直すこと。

日次の全件巡回は [ROUTINE_PROMPT.md](ROUTINE_PROMPT.md) を参照。

GUI: <https://claude.ai/code/artifact/de9706cf-f912-4d45-ac25-efa06579b455>

## スケジュールの書き方に注意

cron は `0 0-22 * * *` にしてある。理由が 2 つある。

`0 * * * *` と書くと、サーバが「毎時」と解釈して負荷分散のため
登録時の分に自動でずらしてしまう（実際 48 分、次いで 57 分にずらされた）。
時刻を範囲で書くとそのまま保存される。

23 時（UTC。日本時間の 8 時）を範囲から外しているのは、日次の全件巡回と
同時刻になるのを避けるため。両方が動くと 2 つのセッションが
`state/seen.json` を同時に push して衝突する。その時間帯は全件巡回が兼ねる。

## 大半の回は何もしない

これが設計の要。1 日 23 回発火するので、空振りを安く済ませないと日次巡回より
高くつく。**リポジトリを clone する前に**、DB を 2 回読んで判定だけする。

| 条件 | 動作 |
|---|---|
| `control/rescan` の `requested` が `true` | 全件モード |
| watchlist に `searched: false` の項目がある | 差分モード（その項目だけ） |
| どちらでもない | 即終了。clone もコミットも通知もしない |

## X（Twitter）を引く

公式アカウントの告知はニュースサイトより早いので、必ず実行する。

```bash
cd /home/user/maniax && python3 -m aew.sources --watchlist /tmp/watchlist.json --x
```

出力されたクエリは、WebSearch の `allowed_domains` に
`["x.com", "twitter.com"]` を指定して投げる。投稿本文が結果のタイトルとして
そのまま返るので、それを `title` と `snippet` の両方に入れる。

**`published` は空でよい。** status URL の ID から投稿日時が厳密に復元される。
推測で埋めると、確実な値を上書きしてしまう。

## 本文照合も通す

`ingest` のあと、必ず次を実行する。

```bash
cd /home/user/maniax && python3 -m aew.confirm --digest /tmp/digest.json --out /tmp/digest.json
```

本文が読めれば、スニペットに無い年を本文から取って日付を訂正する。
読めない環境では「本文未取得」と記録するだけで何も壊さない。

## 印の付け替えを飛ばさない

新着が 0 件でも、`searched: true` の書き戻しと `control/rescan` のリセットは
必ず行う。ここを飛ばすと同じ項目を毎時間検索し続けることになる。

## 2 つの巡回が状態を共有している

重複判定はどちらも `state/seen.json` を使う。差分巡回が拾ったイベントは
日次巡回では再通知されないし、その逆も同じ。どちらも `--commit` を付けて
実行し、必ず push すること。

## 収集時の鉄則

[COLLECTION_RULES.md](COLLECTION_RULES.md) を必ず守ること。要点:

- `published`（記事の公開日）を入れる。分からなければ空にし、**推測で埋めない**
- `snippet` に検索結果の文面をそのまま入れる。日付はここからしか読まない
- **要約に、出典が書いていない年を補わない**（「1月27日」を「2027年1月27日」にしない）
- `published` が半年より古い記事は候補に入れない
